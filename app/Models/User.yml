definitions:
  User:
    type: object
    properties:
      id:
        type: uint
      username:
        type: string
      email:
        type: string
      password:
        type: string
      profileImg:
        type: string
      description:
        type: string
    required:
      - username
      - email
      - password
      - profileImg
      - description
