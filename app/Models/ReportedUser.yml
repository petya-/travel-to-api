definitions:
  ReportedUser:
    type: object
    properties:
      id:
        type: integer
      reason:
        type: string
      approved:
        type: boolean
      reporter_id:
        type: integer
      reported_id:
        type: integer
    required:
      - reason
      - reporter_id
      - reported_id
