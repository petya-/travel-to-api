definitions:
  Car:
    type: object
    properties:
      id:
        type: integer
      license_number:
        type: string
      model:
        type: string
      brand:
        type: string
      color:
        type: string
      driver_id:
        type: integer
    required:
      - model
      - color
      - driver_id
