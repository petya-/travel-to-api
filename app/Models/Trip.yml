definitions:
  Trip:
    type: object
    properties:
      id:
        type: integer
      from:
        type: string
      to:
        type: string
      departure_time:
        type: string
        format: date-time
      number_of_passengers:
        type: integer
      price:
        type: number
      rules:
        type: object
    required:
      - from
      - to
      - name
      - departure_time
      - number_of_passengers
      - price
